有一个按时间排列的order event data有col streamTime, orderId, executedNotionalSize, side, messageType. 现在我想加一个新的col叫做waitingTime, 定义是对于同一个的orderId，时间<=最相近的row的messageType为OrderRequest或者OrderReplaceRequest对应的streamTime和这个row streamTime的difference，第二个col叫做cumExecutedNotionalSize，是对于同一个的orderId，时间<=最相近的row的messageType为OrderRequest或者OrderReplaceRequest对应的row开始算到这个row executedNotionalSize的累积值
